class Solution {
    public int searchInsert(int[] nums, int target) {
       int left =0;
       int right = arr.length-1; 
       while(left<right){
        int sum= num[left]+num[right];
        if (sum==target){
            return (left+right)/2;

        }
        else if( )
       } 
    }
}